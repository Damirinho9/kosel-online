# ADR-0003: TensorFlow.js несовместим с Chrome Extension Manifest V3

**Дата:** 2024-11-25
**Статус:** Принято (ML функции отключены)
**Контекст:** V2.0 - Попытка интеграции TensorFlow.js в Chrome Extension Manifest V3

## Контекст и проблема

Козёл Помощник V2.0 планировал использовать TensorFlow.js для ML предсказаний ходов. После обширного исследования и множественных попыток различных подходов, выявлена **фундаментальная несовместимость** TensorFlow.js с Chrome Extension Manifest V3 CSP ограничениями.

## Попытки решения

### Попытка 1: Стандартный TensorFlow.js (tf.min.js) в offscreen document

**Идея:** Использовать offscreen document API для обхода Service Worker CSP ограничений

**Реализация:**
- Загрузили полный tf.min.js (1.4 MB)
- Создали offscreen.html с TensorFlow.js
- Переместили ML операции в offscreen context

**Проблема:**
```
Uncaught EvalError: Evaluating a string as JavaScript violates the following
Content Security Policy directive because 'unsafe-eval' is not an allowed
source of script: script-src 'self'
```

**Причина:**
- TensorFlow.js стандартная версия использует `eval()` внутри
- Manifest V3 CSP **не разрешает** `'unsafe-eval'` даже в offscreen documents
- Попытка добавить `'unsafe-eval'` в manifest.json CSP отклоняется Chrome как insecure

**Результат:** ❌ FAILED

---

### Попытка 2: Модульная WASM версия (tf-core + tf-backend-wasm)

**Идея:** Использовать WASM backend TensorFlow.js который не использует `eval()`

**Реализация:**
- Загрузили модульные файлы:
  - `tf-core.min.js` (284 KB)
  - `tf-backend-wasm.min.js` (147 KB)
  - `tfjs-backend-wasm.wasm` (304 KB)
- Создали offscreen-wasm.html для модульной загрузки
- Создали tf-wasm-init.js для инициализации

**Проблема 1 - WASM CSP:**
```
WebAssembly.instantiate(): violates CSP because neither 'wasm-eval'
nor 'unsafe-eval' is an allowed source
```

**Решение 1:** Добавили `'wasm-unsafe-eval'` в CSP (это разрешено Chrome)

**Проблема 2 - Web Workers:**
```
Loading the script 'blob:<URL>' violates the following Content Security
Policy directive: "script-src 'self' 'wasm-unsafe-eval'"

worker sent an error! Failed to execute 'importScripts' on 'WorkerGlobalScope'
```

**Причина:**
- WASM backend с threading создает Web Workers
- Workers загружаются через `blob:` URLs
- `blob:` не разрешен в CSP для Manifest V3

**Решение 2:** Попытались добавить `blob:` в CSP

**Результат решения 2:**
```
'content_security_policy.extension_pages': Insecure CSP value "blob:"
in directive 'script-src'.
```

Chrome **отклоняет** манифест с `blob:` как insecure.

**Результат:** ❌ FAILED

---

### Попытка 3: WASM single-threaded режим

**Идея:** Отключить Web Workers в WASM backend, использовать single-threaded режим

**Реализация:**
```javascript
// Отключаем threading явно
if (tf.env && tf.env().set) {
    tf.env().set('WASM_HAS_MULTITHREAD_SUPPORT', false);
    tf.env().set('WASM_HAS_SIMD_SUPPORT', false);
}
```

**Проблема:**
WASM backend **всё равно создает workers**, несмотря на флаги. Web Workers встроены в `tf-backend-wasm.min.js` и создаются автоматически.

**Результат:** ❌ FAILED

---

## Анализ всех подходов

| Подход | eval() | Web Workers | blob: URLs | CSP статус | Результат |
|--------|--------|-------------|------------|------------|-----------|
| Стандартный tf.min.js | ❌ Использует | ⚠️ Возможно | ⚠️ Возможно | ❌ 'unsafe-eval' запрещен | **FAILED** |
| WASM threaded | ✅ Не использует | ❌ Использует | ❌ Использует | ❌ blob: запрещен | **FAILED** |
| WASM single-threaded (попытка) | ✅ Не использует | ❌ Создает anyway | ❌ Создает anyway | ❌ blob: запрещен | **FAILED** |
| Удаленный сервер | ✅ Не нужен | ✅ Не нужны | ✅ Не нужны | ✅ Совместимо | ⚠️ Требует инфраструктуру |

## Решение

**Принято решение:** Отключить ML функции до появления Manifest V3 совместимой версии TensorFlow.js.

### Обоснование

1. **TensorFlow.js фундаментально несовместим** с Manifest V3 CSP ограничениями:
   - Стандартная версия требует `'unsafe-eval'` (запрещено)
   - WASM версия требует Web Workers через `blob:` (запрещено)
   - Chrome **не разрешает** эти CSP значения в Manifest V3

2. **Расширение полностью функционально без ML:**
   - ✅ Адаптивные эвристические стратегии V2.0 работают
   - ✅ Профилирование игроков работает
   - ✅ Паттерн-детекция работает
   - ✅ Все game logic работает
   - ✅ Graceful degradation: если ML недоступен, используются эвристики

3. **Upstream issue:** TensorFlow.js команда знает о проблеме:
   - GitHub issue: https://github.com/tensorflow/tfjs/issues/5429
   - Пока нет решения или timeline

4. **Альтернатива (удаленный ML сервер):**
   - ❌ Требует backend инфраструктуру (hosting, costs)
   - ❌ Проблемы с приватностью (отправка игровых данных)
   - ❌ Network latency для каждого предсказания
   - ❌ Усложнение архитектуры

## Последствия

### Положительные:

1. **Расширение работает стабильно** без CSP проблем
2. **Эвристики V2.0 эффективны** без ML:
   - Профилирование стиля игры
   - Паттерн-матчинг
   - Контроль козырей
   - Максимизация взяток
3. **Простота установки** - не нужно скачивать TensorFlow.js
4. **Меньше потребление памяти** - нет TensorFlow.js (~30-50 MB)
5. **Быстрый запуск** - нет инициализации ML

### Отрицательные:

1. **ML функции недоступны** - нет нейросетевых предсказаний
2. **Нет обучения** на истории игр
3. **Менее адаптивный** к уникальным ситуациям

### Технические изменения:

1. **background.js** - используется `offscreen.html` с graceful degradation
2. **offscreen.js** - логирует недоступность TensorFlow.js, продолжает работу
3. **strategy.js** - использует только эвристики (уже реализовано как fallback)
4. **Документация** - обновлена с объяснением ограничения

## Будущее

### Когда TensorFlow.js получит Manifest V3 support:

1. Проверить новую версию TensorFlow.js
2. Обновить tf.min.js или WASM files
3. Протестировать в offscreen document
4. Включить ML функции обратно
5. Обновить этот ADR статус на "Устарело"

### Мониторинг:

- GitHub issue: https://github.com/tensorflow/tfjs/issues/5429
- TensorFlow.js releases: https://github.com/tensorflow/tfjs/releases
- Chrome Extension Manifest V3 updates

## Проверка решения

### Definition of Done:

✅ Расширение работает без ошибок CSP
✅ Эвристики V2.0 предоставляют рекомендации
✅ Graceful degradation: ML недоступен но не блокирует работу
✅ Документация обновлена
✅ Пользователи понимают ограничение
✅ ADR документирует причины и попытки

### Логи (ожидаемые):

```
[Background] ✓ Offscreen document создан для ML
[ML Offscreen] Документ загружен
[ML Offscreen] ✗ TensorFlow.js несовместим с Manifest V3 CSP
[ML Offscreen] ⚠️ ML функции недоступны из-за ограничений Chrome Extension
[ML Offscreen] Расширение продолжит работать без ML предсказаний
[ML Offscreen] Подробнее: https://github.com/tensorflow/tfjs/issues/5429
[ML Offscreen] ✓ Готов к обработке ML запросов
[AI Strategy] Используем эвристики V2.0 (ML недоступен)
[AI Strategy] 🎯 Рекомендую: ♠A (профилирование: агрессивный стиль)
```

---

## Связанные документы

- [ADR-0001: Offscreen Document для обхода CSP](0001-offscreen-document-for-ml.md) - Предыдущая попытка
- [Architecture: ML Components](../architecture.md#5-ml-components-aiml-js)
- [TensorFlow.js Manifest V3 Issue](https://github.com/tensorflow/tfjs/issues/5429)

---

## История изменений

| Дата       | Версия | Изменение                           |
|------------|--------|-------------------------------------|
| 2024-11-25 | 1.0    | Первая версия ADR (ML отключен)    |

---

**Автор:** AI Dev Agent (Claude)
**Исследование:** Множественные попытки различных подходов (2024-11-25)
**Решение:** Отключить ML до появления Manifest V3 support в TensorFlow.js
