# 🧪 Руководство по тестированию V2.0

## Что нового в V2.0

### ✨ Основные функции
1. **Профилирование игроков** - AI анализирует стиль игры партнёра и противников
2. **Адаптивные стратегии** - AI адаптирует свою тактику в зависимости от стиля игроков
3. **История ходов** - Запись всех ходов для анализа и обучения
4. **Расширенное логирование** - Подробные логи для отладки

### 🎭 Типы игроков
AI определяет 5 стилей игры:
- **⚔️ Aggressive** - агрессивный, много берёт взяток
- **🛡️ Defensive** - защитный, осторожный
- **🎲 Risky** - рискованный, идёт на сложные взятки
- **💪 Assertive** - напористый, уверенный
- **⚖️ Balanced** - сбалансированный

### 📊 Адаптация стратегии

#### Если партнёр агрессивный:
- AI подбрасывает ему больше очковых карт
- Агрессивность AI +20%

#### Если партнёр защитный:
- AI играет более агрессивно для компенсации
- Агрессивность AI +10%

#### Если противники агрессивные:
- AI играет более защитно
- Защитность AI +15%

#### Если противники защитные:
- AI может играть более агрессивно
- Агрессивность AI +15%

## 🔍 Как тестировать

### 1. Установка расширения
```bash
1. Откройте chrome://extensions/
2. Включите "Режим разработчика"
3. "Загрузить распакованное расширение"
4. Выберите папку kozel-assistant/
```

### 2. Проверка базовой работы
1. Откройте https://kozel-online.com/
2. Начните игру
3. Откройте консоль (F12)
4. Проверьте логи `[Козёл Помощник V2.0]`

**Ожидаемые логи:**
```
[Козёл Помощник] Инициализация...
[Козёл Помощник V2.0] Профайлер загружен
[Козёл Помощник V2.0] История ходов активирована
[Козёл Помощник] ✓ Готов к работе!
```

### 3. Проверка детекции ходов
Сделайте несколько ходов и проверьте консоль:

**Ожидаемые логи:**
```
[Козёл V2.0] Обнаружен ход: left сыграл 7♣
[Козёл V2.0] ✓ Ход записан в профайлер: PlayerName
[Козёл V2.0] ✓ Свой ход записан в историю
```

### 4. Проверка профилирования
После 5-10 ходов каждого игрока:

1. Откройте popup расширения
2. Проверьте раздел **🎭 Профили игроков**
3. Должны отображаться стили игроков с эмодзи

**Пример:**
```
🎭 Профили игроков
🤝 ИмяПартнёра
   ⚔️ Агрессивный - берёт много взяток

👈 Игрок1
   🛡️ Осторожный - играет защитно

👉 Игрок2
   🎲 Рискованный - идёт на сложные взятки
```

### 5. Проверка адаптации AI
В консоли смотрите логи стратегии:

**Ожидаемые логи:**
```
[AI V2.0] Базовая ситуация: {playAggressive: 0.40, playDefensive: 0.30}
[AI V2.0] Профиль партнёра: PlayerName - aggressive (85%)
[AI V2.0] → Партнёр агрессивный: +0.2 агрессия
[AI V2.0] ⚙️ Адаптация стратегии: {
  before: {agg: 0.40, def: 0.30},
  after: {agg: 0.60, def: 0.30}
}
[AI V2.0] ✓ Выбрана стратегия: aggressive → K♥
```

### 6. Проверка хранения данных
После завершения игры:

```javascript
// В консоли браузера:
chrome.storage.local.get(['kozel_player_profiles', 'kozel_move_history'], (data) => {
    console.log('Профили:', data.kozel_player_profiles);
    console.log('История:', data.kozel_move_history);
});
```

## ✅ Чек-лист тестирования

- [ ] Расширение успешно загружено
- [ ] V2.0 модули инициализированы (логи)
- [ ] Ходы игроков детектируются
- [ ] Профили записываются в storage
- [ ] История ходов сохраняется
- [ ] UI показывает профили игроков
- [ ] Профили отображаются в popup
- [ ] AI адаптирует стратегию (логи)
- [ ] Профили сохраняются между играми
- [ ] После 10+ ходов появляются стили игроков

## 🐛 Известные ограничения

1. **Требуется минимум 6 ходов** для определения стиля игрока (confidence > 30%)
2. **Профили сбрасываются** при очистке chrome.storage
3. **История ограничена** последними 500 ходами и 50 играми
4. **Детекция работает только на kozel-online.com**

## 📈 Метрики успеха

Хорошая работа V2.0 определяется по:

1. **Точность профилирования**: > 70% после 20 ходов
2. **Адаптация стратегии**: Видимые изменения в весах
3. **Win Rate**: Должен расти со временем
4. **Успешность рекомендаций**: > 60% взяток по рекомендациям

## 🔧 Отладка

### Проблема: Профили не определяются
**Решение:**
- Проверьте, что игроки делают достаточно ходов (минимум 6)
- Проверьте chrome.storage на наличие данных
- Перезагрузите расширение

### Проблема: Ходы не записываются
**Решение:**
- Проверьте логи `[Козёл V2.0]` в консоли
- Убедитесь что помощник включен
- Проверьте что inject.js загрузился

### Проблема: AI не адаптируется
**Решение:**
- Проверьте что confidence профилей > 0.3
- Проверьте логи `[AI V2.0]` для адаптации
- Дождитесь большего количества ходов

## 📊 Просмотр сохранённых данных

### Профили игроков
```javascript
chrome.storage.local.get(['kozel_player_profiles'], (data) => {
    const profiles = data.kozel_player_profiles || {};
    for (const [name, profile] of Object.entries(profiles)) {
        console.log(`${name}:`, {
            gamesPlayed: profile.gamesPlayed,
            aggressiveness: profile.aggressiveness.toFixed(2),
            riskTaking: profile.riskTaking.toFixed(2),
            style: profile.analysis?.style
        });
    }
});
```

### История ходов
```javascript
chrome.storage.local.get(['kozel_move_history'], (data) => {
    const history = data.kozel_move_history || {};
    console.log('Всего игр:', history.games?.length || 0);
    console.log('Всего ходов:', history.allMoves?.length || 0);

    // Последняя игра
    if (history.games?.length > 0) {
        const lastGame = history.games[0];
        console.log('Последняя игра:', {
            result: lastGame.result,
            moves: lastGame.moves.length,
            finalScore: lastGame.finalScore
        });
    }
});
```

### Статистика рекомендаций AI
```javascript
// В консоли после инициализации MoveHistory
if (typeof MoveHistory !== 'undefined') {
    const history = new MoveHistory();
    history.analyzeAIPerformance().then(stats => {
        console.log('Производительность AI:', stats);
    });
}
```

## 🎯 Следующие шаги (Phase 3 - опционально)

Если V2.0 работает хорошо, можно добавить:
- **Machine Learning** с TensorFlow.js
- **Предсказание ходов** противников
- **Оптимизация весов** на основе истории
- **A/B тестирование** разных стратегий
- **Экспорт статистики** в JSON/CSV

---

**Версия:** V2.0 Phase 2
**Дата:** 2025-11
**Статус:** ✅ Готово к тестированию
