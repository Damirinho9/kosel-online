# 🧠 Machine Learning Guide - Козёл Помощник V2.0

## Обзор

V2.0 Phase 3 добавляет полноценную систему Machine Learning с использованием TensorFlow.js для предсказания лучших ходов на основе исторических данных.

## Архитектура

### 🔧 Компоненты

1. **ml-loader.js** - Загрузка TensorFlow.js
   - Динамическая загрузка с CDN (jsdelivr)
   - Проверка доступности библиотеки
   - Обработка ошибок загрузки

2. **ml-encoder.js** - Кодирование состояния игры
   - Преобразование карт в one-hot векторы
   - Нормализация числовых признаков
   - 159-мерный входной вектор

3. **ml-model.js** - Нейронная сеть
   - Sequential модель TensorFlow.js
   - 4 слоя + dropout
   - Adam optimizer
   - Категориальная кросс-энтропия

### 🧬 Архитектура нейронной сети

```
Входной слой (159 признаков):
├─ Карты на руке (36 one-hot)
├─ Карты на столе (36 one-hot)
├─ Позиция игрока (4 one-hot)
├─ Счёт (2 нормализованных)
├─ Мой ход (1 boolean)
├─ Кто берёт взятку (3 one-hot)
└─ Дополнительно (10 признаков)

↓
Dense(128) + ReLU
↓
Dropout(0.2)
↓
Dense(64) + ReLU
↓
Dropout(0.2)
↓
Dense(32) + ReLU
↓
Dense(36) + Softmax

Выходной слой (36 вероятностей):
└─ Вероятность для каждой карты (0-1)
```

## Кодирование признаков

### Входные признаки (159 размерность)

1. **Карты на руке (36)**
   ```javascript
   // One-hot вектор для каждой карты в колоде
   [0,0,0,1,0,0,...] // 7♥ на руке
   ```

2. **Карты на столе (36)**
   ```javascript
   // One-hot вектор для карт на столе
   [0,1,0,0,1,0,...] // Две карты на столе
   ```

3. **Позиция игрока (4)**
   ```javascript
   [1,0,0,0] // bottom (мы всегда bottom)
   ```

4. **Счёт (2)**
   ```javascript
   [myScore/120, oppScore/120] // Нормализовано к [0,1]
   ```

5. **Мой ход (1)**
   ```javascript
   [1] или [0] // Boolean
   ```

6. **Кто берёт взятку (3)**
   ```javascript
   [1,0,0] // nobody
   [0,1,0] // partner/we
   [0,0,1] // opponent
   ```

7. **Дополнительные признаки (10)**
   - Карт на руке (нормализовано)
   - Карт на столе (нормализовано)
   - Выигранных партий
   - Проигранных партий
   - 6 резервных признаков

### Выходной вектор (36 размерность)

```javascript
[0.02, 0.15, 0.48, ...] // Вероятности для каждой карты
```

Индекс карты = `suitIndex * 9 + rankIndex`

## Обучение

### Процесс обучения

1. **Сбор данных**
   - Каждый ход записывается в `MoveHistory`
   - Сохраняется состояние до хода, сыгранная карта, результат

2. **Подготовка данных**
   ```javascript
   {
       state: [159-мерный вектор],
       action: [индекс карты 0-35],
       reward: [0.0 - 1.0]
   }
   ```

3. **Система вознаграждений**
   ```javascript
   reward = 0.5  // Базовая награда

   if (trickWon) reward += 0.2  // Взяли взятку
   if (points > 0) reward += min(0.3, points/30)  // Очки

   // Финальная награда с учётом результата игры
   reward = reward * 0.7 + gameResult * 0.3
   ```

4. **Обучение**
   - Автоматическое после каждой игры
   - Используется последние 5 игр
   - 10 эпох, batch size 32
   - Валидация 20%
   - Модель сохраняется в IndexedDB

### Ручное обучение

В popup есть кнопка **"🧠 Обучить ML"** для ручного запуска обучения:
1. Нажмите кнопку
2. Модель обучится на последних 5 играх
3. Результаты отобразятся в консоли
4. Модель автоматически сохранится

## Гибридный AI

Система использует комбинацию ML и эвристик:

```
                  ┌─────────────┐
                  │ Легальные   │
                  │   карты     │
                  └──────┬──────┘
                         │
              ┌──────────▼──────────┐
              │   ML Предсказание   │
              │  (если доступен)    │
              └──────────┬──────────┘
                         │
           ┌─────────────┴─────────────┐
           │                           │
      Confidence > 80%          Confidence < 80%
           │                           │
      ┌────▼────┐                ┌────▼────┐
      │   ML    │                │ Эврист. │
      │ решение │                │ решение │
      └─────────┘                └─────────┘
```

### Логика выбора

1. **Уверенность > 80%**: Используется чистый ML
   ```javascript
   return {
       card: mlPrediction.card,
       reasoning: `🧠 ML: 85% уверенности`
   };
   ```

2. **Уверенность 60-80%**: ML учитывается, но проверяется эвристиками
   - ML даёт рекомендацию
   - Эвристики корректируют если необходимо

3. **Уверенность < 60%**: Используются эвристики V2.0
   - Адаптивные стратегии
   - Профилирование игроков
   - Правила игры

## Использование

### Автоматический режим

ML активируется автоматически при наличии обученной модели:
1. Расширение загружается
2. TensorFlow.js скачивается с CDN
3. Модель загружается из IndexedDB
4. ML начинает работать

### Требования

- ✅ **Минимум 1 завершённая игра** для обучения
- ✅ **5+ игр** для хорошего качества
- ✅ **20+ игр** для отличного качества
- ✅ **50+ игр** для максимальной точности

### Мониторинг

**В консоли браузера (F12):**
```
[Козёл Помощник ML] ✓ ML модель загружена
[Козёл Помощник ML] Статистика: {predictions: 0, trainingSessions: 0}
[AI ML] 🧠 ML рекомендует: K♥ (85.3%)
[Козёл ML] Начинаем обучение после завершения игры...
[KozelML] Epoch 1: loss=0.5432, acc=0.7123
[KozelML] ✓ Обучение завершено. Финальная loss: 0.3215
```

**В popup:**
```
🧠 Machine Learning
Статус: ✓ Активен
Предсказаний: 42
Обучений: 5
Loss: 0.3215
```

## Производительность

### Метрики

- **Predictions**: Количество ML предсказаний
- **Training Sessions**: Количество обучений
- **Loss**: Ошибка обучения (меньше = лучше)
  - < 0.5: отлично
  - 0.5-1.0: хорошо
  - > 1.0: нужно больше игр

### Оптимизация

1. **Размер модели**: ~100KB в IndexedDB
2. **Скорость предсказания**: < 50ms
3. **Скорость обучения**: ~5-10 секунд на 5 игр
4. **TensorFlow.js**: ~2MB загрузка при первом запуске

## Хранение данных

### IndexedDB

```
kozel-ml-model/
├─ model.json        // Архитектура модели
├─ weights.bin       // Веса нейронной сети
└─ metadata.json     // Метаданные

chrome.storage.local/
└─ kozel_ml_stats    // Статистика ML
```

### Экспорт данных

```javascript
// В консоли браузера
const assistant = /* получить экземпляр */;
const mlData = await assistant.moveHistory.exportMLData();
console.log(mlData);
// {metadata, games, trainingData}

// Сохранить в файл
const json = JSON.stringify(mlData, null, 2);
const blob = new Blob([json], {type: 'application/json'});
const url = URL.createObjectURL(blob);
// Скачать url
```

## Отладка

### Логи

```javascript
// Включить подробные логи TensorFlow.js
tf.ENV.set('DEBUG', true);

// Просмотр модели
mlModel.model.summary();

// Статистика
console.log(mlModel.getStats());
```

### Сброс модели

```javascript
// В консоли браузера после получения доступа к assistant
await assistant.mlModel.resetModel();
// Модель будет пересоздана с нуля
```

### Проблемы и решения

#### ML не загружается

**Проблема**: `ML отключен или загружается...`

**Решение**:
1. Проверьте интернет (TensorFlow.js загружается с CDN)
2. Откройте консоль (F12) и проверьте ошибки
3. Проверьте CSP в manifest.json
4. Перезагрузите расширение

#### Низкая точность

**Проблема**: Loss > 1.0, плохие предсказания

**Решение**:
1. Сыграйте больше игр (20+)
2. Следуйте рекомендациям AI
3. Нажмите "Обучить ML" вручную
4. Проверьте что данные записываются

#### Ошибки обучения

**Проблема**: `Ошибка обучения`

**Решение**:
1. Проверьте что есть завершённые игры
2. Проверьте консоль на ошибки TensorFlow.js
3. Попробуйте сбросить модель
4. Убедитесь что браузер не блокирует IndexedDB

## Расширенное использование

### Кастомные веса наград

Отредактируйте `move-history.js`:
```javascript
let reward = 0.5;  // Базовая награда

if (move.trickWon) {
    reward += 0.2;  // ← Изменить вес за взятку
    if (move.pointsGained > 0) {
        reward += Math.min(0.3, move.pointsGained / 30);  // ← Вес за очки
    }
}

// Вес результата игры
reward = reward * 0.7 + gameReward * 0.3;  // ← 70/30 микс
```

### Изменение архитектуры

Отредактируйте `ml-model.js`:
```javascript
tf.layers.dense({
    units: 128,  // ← Количество нейронов
    activation: 'relu',
    ...
})
```

### Настройка обучения

Отредактируйте `ml-model.js`:
```javascript
constructor() {
    this.learningRate = 0.001;  // ← Скорость обучения
    this.batchSize = 32;        // ← Размер батча
    this.epochs = 10;           // ← Количество эпох
}
```

## Будущие улучшения

Возможные направления развития:

1. **Transfer Learning**
   - Предобученная модель на тысячах игр
   - Fine-tuning под конкретного игрока

2. **Reinforcement Learning**
   - Q-Learning или Actor-Critic
   - Самообучение через симуляции

3. **Opponent Modeling**
   - Предсказание ходов противников
   - Адаптация в реальном времени

4. **Ensemble Methods**
   - Несколько моделей голосуют
   - Повышение надёжности

5. **Feature Engineering**
   - Более сложные признаки
   - Комбинации карт
   - Исторические паттерны

---

**Версия:** V2.0 Phase 3
**Дата:** 2025-11
**TensorFlow.js:** 4.11.0
**Статус:** ✅ Полностью функциональна
